<%= provide(:title, t('.resend_unlock_instructions')) %>

<div class="userform-box">
  <div class="userform-content center">
    <h2><%= t('.resend_unlock_instructions') %></h2>

    <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
      <%= render 'layouts/error_messages', model: resource %>

      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>

      <div class="actions">
        <%= f.submit t('.resend_unlock_instructions') %>
      </div>
    <% end %>

    <%= render "users/shared/links" %>
  </div>
</div>
