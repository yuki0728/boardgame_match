<% @events.each do |event| %>
  <li>
    <%= link_to event_path(event) do %>
      <%= event.name %>
      <%= event.text %>
      <%=  l(@event.start_time , format: :long) %>
      <%=  l(@event.ending_time , format: :long) %>
    <% end %>
  </li>
<% end %>